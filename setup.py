from setuptools import setup

setup(
    name='eth_async',
    version='1.0',
    description='A useful module',
    author='13',
    author_email='author@gmail.com',
    packages=['eth_async'],  # same as name
    install_requires=[],  # external packages as dependencies
)